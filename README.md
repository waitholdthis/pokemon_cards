# Pokémon Cards

Mobile-first scanner + collection tracker with live market values.

## Repo layout
- `backend/` — Node/Express API + TCGPlayer pricing pipeline
- `frontend/` — Expo React Native app
- `migrations/` — Postgres schema versions
