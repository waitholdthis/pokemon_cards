import { View, Text } from 'react-native';

export default function WatchlistScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Watchlist coming next.</Text>
    </View>
  );
}
